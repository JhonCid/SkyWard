const stars=new THREE.BufferGeometry(),starArr=[];const sr=rng(99);for(let i=0;i<4500;i++){let v=V(sr()*2-1,sr()*2-1,sr()*2-1).normalize().multiplyScalar(90000);starArr.push(v.x,v.y,v.z)}stars.setAttribute('position',new THREE.Float32BufferAttribute(starArr,3));const starsObj=new THREE.Points(stars,new THREE.PointsMaterial({color:0xc5e3ff,size:55,sizeAttenuation:true,transparent:true,depthWrite:false,depthTest:true,fog:false}));starsObj.renderOrder=-9999;scene.add(starsObj);
